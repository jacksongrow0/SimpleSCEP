package components

templ Card(props CardProps) {
	<div { mergeAttrs("card overflow-hidden rounded-xl border border-border bg-card shadow-[0_1px_3px_#0002]", props.Attrs)... }>
		{ children... }
	</div>
}
