package components

templ Field(props FieldProps) {
	<label { mergeAttrs("flex flex-col gap-2 text-sm font-medium", props.Attrs)... }>
		{ props.Label }
		{ children... }
		if props.Hint != "" {
			<small class="font-normal text-muted-foreground">{ props.Hint }</small>
		}
	</label>
}
