package components

templ Icon(name string) {
	if name != "" {
		<svg class="size-4 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
			@templ.Raw(iconPaths[name])
		</svg>
	}
}
